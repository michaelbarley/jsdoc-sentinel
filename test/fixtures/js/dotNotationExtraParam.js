/**
 * Updates a user record.
 * @param user the whole user object
 * @param user.name UNUSED – obsolete
 * @returns updated user
 */
function update(user) {
  return user;
} // no “user.name” param in code
