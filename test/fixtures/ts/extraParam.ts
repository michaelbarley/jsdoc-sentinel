/**
 * Multiplies two numbers.
 * @param a first
 * @param b second
 * @param c UNUSED – obsolete
 * @returns product
 */
export function multiply(a: number, b: number): number {
  return a * b; // “c” removed
}
